import styled from "styled-components";

const HeaderEstilizado = styled.header`
    padding: 60px 0;
    display: flex;
`;

const Cabecalho () => {
    return (
        <HeaderEstilizado>
        
        </HeaderEstilizado>
    );
}